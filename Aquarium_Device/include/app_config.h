#pragma once

// Default configuration values (can later be synced from cloud commands)

#define APP_LOOP_DELAY_MS 500U

// Servo pulse width (microseconds) for 0° / 180° (typical)
#define SERVO_PULSE_MIN_US 1000U
#define SERVO_PULSE_MAX_US 2000U

