# 实物联调记录

本文档记录 2026-03-10 这一轮实物接线、烧录、联网与传感器排障的现场结论。它不是“理想接线图”，而是当前桌面实测进度与问题台账。

## 1. 当前接线进度

### 已接好并完成基本验证

| 模块 | 当前接线 | 现场结论 |
|------|----------|----------|
| NUCLEO-F103RB | 板载 USB 供电 | 可正常烧录与复位 |
| OLED | `SCL -> D15(PB8)`，`SDA -> D14(PB9)`，`VCC -> 3V3`，`GND -> GND` | 已点亮，可显示页面与联网状态 |
| ESP32-WROOM(AT) | `GPIO17 -> D2(PA10/RX1)`，`GPIO16 -> D8(PA9/TX1)`，`GND -> 共地`，`3V3 -> 3.3V` | 已恢复 AT 固件并能拉起 AP 配网页面 |
| DS18B20 | `黄线 -> D7(PA8)`，`红线 -> 3V3`，`黑线 -> GND`，`D7` 与 `3V3` 之间串 `4.7kΩ` 上拉 | 已验证可测温，手捏探头时温度会上升 |
| 水位传感器 | `VCC -> 3V3`，`GND -> GND`，`AO -> A3(PB0)` | 已验证离水约 0%，入水后数值会上升 |

### 当前已拔掉或暂停验证

| 模块 | 原计划接线 | 当前状态 |
|------|------------|----------|
| pH 模块 | `V+ -> 3.3V`，`G -> GND`，`Po -> A0(PA0)` | 已暂时拔线，待换货/复检 |
| TDS 模块 | `+ -> 3.3V`，`- -> GND`，`AO -> A1(PA1)` | 已暂时拔线，待补拍视频给商家 |
| TS-300B 浊度 | `AO -> A2(PA4)` | 这一轮未继续接入，暂缓 |

## 2. 已验证通过的现场结果

1. `PlatformIO` 设备侧构建成功，且已通过 `ST-LINK` 完成多次烧录。
2. OLED 在烧录并正确接线后可以点亮。
3. ESP32 AT 固件已重刷，默认 AP 为 `Aquarium_Setup`，固定密码为 `12345678`。
4. DS18B20 在补上 `4.7kΩ` 上拉并重新压紧接线后恢复正常，不再固定停在 `85°C`。
5. 水位传感器已能区分“离水”和“浸水”，当前代码里做了演示向标定。
6. 设备曾实测进入 `NET=ONLINE`，并在华为云控制台出现过 `ONLINE` 状态。

## 3. 本轮联调中确认的问题与挫折

### pH 模块

- 先后测试过自来水、白醋、BNC 短接等办法，读数长期停在 `2.5 ~ 2.9` 左右，偶发跳到 `14`、`9.x`、`5.x`，表现不稳定。
- 将模块供电从 `5V` 改到 `3.3V` 后，读数有变化，但仍不可信。
- 按商家建议调节靠近 `BNC` 的电位器多圈后，仍未得到可复现的正常响应。
- 当前判断：`pH` 模块或电极链路高度可疑，已按“疑似坏件/需售后”记录，后续应优先换货再测。
- 2026-03-12 资料复核补充：重新读取卖家附带说明书后，确认该模块官方用法并不是“上电直接看数”。资料明确写了三点：1) 模块供电推荐 `+5.00V`；2) 先在 `pH 7.00` 标液中或直接短接 `BNC` 两输入，记录程序打印值并通过 `Offset` 做零点修正；3) 再放入 `pH 4.00` 或 `pH 9.18` 标液，调节增益电位器做酸段/碱段校准。因此，当前只能说“此前 3.3V 直读 `2.8~3.0` 的结果不足以最终判死”，需要先按说明书条件重测，代码侧也要核对换算公式是否与该模块资料一致。

### TDS 模块

- 按 `AO/A1`、`3.3V/GND` 接好后，空气中和水中都曾长时间读 `0`。
- 中途出现过一次约 `1623` 的读数，但不能稳定复现。
- 当前判断：`TDS` 模块链路尚未打稳，既可能是模块/探针本体问题，也可能是商家版本与文档不一致，已记录为“待与商家联调视频复核”。

### 浊度模块（TS-300B）

- 这一轮被搁置，尚未形成实测闭环。
- 原因不是代码已确认没问题，而是现场优先级让位给联网、OLED、温度、水位四条主线。

### 水位传感器

- 这一路已经能工作，但“液位高度 -> 百分比”的曲线明显不线性。
- 现场出现过“刚浸入一部分就接近 100%”和“浸入一半仍接近 0，随后突然跳满”的情况。
- 用户最终接受当前版本作为演示版，因此当前水位标定只能视为“可演示”，不能视为“精确测量版”。

### AP 配网 / OLED 现场体验

- iPhone Safari 在访问 `http://192.168.4.1/` 或直接打 `/config?...` 时，出现过白屏或“已丢失网络连接”。
- 最近一轮诊断版固件中，OLED 右上角页码曾长期停在 `P1`，说明页面轮播或主循环仍需继续排查。
- 2026-03-10 10:50 现场复核：在当前最小稳定组合（`NUCLEO + OLED + ESP32 + DS18B20 + 水位传感器`）下，OLED 右上角页码观察 `10` 秒后仍“**一直在 P1**”，说明该问题尚未消失，不是额外传感器接入导致的偶发现象。
- 2026-03-10 10:55 现场复核：按 `RESET` 后 OLED 仍长期停在 `P1`，第 3 行 `U=` 数字观察 `5` 秒后仍固定为 `1`，进一步说明屏幕内容没有继续刷新。
- 2026-03-10 10:58 现场复核：板载 `LD2` 观察 `5` 秒后为“**不闪**”，当前运行态不正常；下一步优先隔离 `ESP32` 链路，确认冻结是否与 `AT/MQTT/AP` 状态机有关。
- 2026-03-10 11:02 现场决策：为避免在异常状态上继续叠加变量，用户已将当前外设接线全部拔掉，接下来按“`NUCLEO` 裸板 -> `OLED` -> `ESP32` -> `DS18B20` -> `水位`”顺序重新建立最小闭环。
- 2026-03-10 11:03 现场复核：仅保留 `NUCLEO-F103RB + USB`，按 `RESET` 后板载 `LD2` 可正常闪烁，说明裸板运行正常；之前的冻结现象与外挂模块/外设链路有关，不是开发板本体故障。
- 2026-03-10 11:05 现场复核：只接回 `OLED`（`PB8/PB9 + 3V3/GND`）后，按 `RESET` 可正常看到 `P1/P2/P3` 轮播，说明 `OLED` 模块本体和 `I2C` 接线正常，之前的冻结问题不由 `OLED` 单独导致。
- 2026-03-10 11:08 现场复核：在 `OLED` 正常轮播的基础上，仅给 `ESP32-WROOM(AT)` 接回 `3V/GND` 供电、暂不接 `GPIO16/17` 串口线时，按 `RESET` 后 `OLED` 仍可正常看到 `P1/P2/P3`，说明冻结问题不由 `ESP32` 供电本身导致，更像是串口链路接入后触发。
- 2026-03-10 11:10 现场复核：继续只加回 `GPIO17 -> D2(PA10/RX1)` 这一根后，按 `RESET` 仍可正常看到 `P1/P2/P3`，说明 `ESP32 TX -> STM32 RX` 这一路单独接入不会触发冻结。
- 2026-03-10 11:12 现场复核：继续加回 `GPIO16 -> D8(PA9/TX1)` 后再次出现“卡 `P1`”，问题稳定复现。该现象更像是 `STM32 <-> ESP32` 真正开始 `AT` 通信后，固件在处理串口返回数据时拖住了主循环，而不是单纯的 `OLED` 或 `ESP32` 供电异常。
- 2026-03-10 11:25 代码侧处理：已为 `ESP32 MQTT` 的 `URC` 处理增加单轮预算，避免 `poll_commands()` / `poll_ap_config()` 在串口持续灌入时长时间独占主循环；本地已通过 `native` 全量测试与 `nucleo_f103rb` 构建，并已重新烧录到现场板卡，待按同一接线方式复测。
- 2026-03-10 11:28 现场复测：在相同全连接条件（`OLED + ESP32电源 + GPIO17 -> PA10 + GPIO16 -> PA9`）下，重新烧录后 OLED 仍“**一直在 P1**”，说明上一轮 `URC` 处理预算修正还不足以解决现场卡屏现象，需继续区分“系统真死住”还是“联网链路在跑、但显示刷新被拖住”。
- 2026-03-10 11:30 现场复测：在上述全连接条件下，手机 Wi‑Fi 列表里暂时看不到 `Aquarium_Setup`。该结果已记录，但因当前固件先尝试现网连接、`AP` 不一定上电立刻出现，仍需继续确认主循环是否在跑。
- 2026-03-10 11:31 现场复测：在同一全连接条件下按 `RESET` 后，板载 `LD2` 观察 `5` 秒为“**会闪**”。这说明 `STM32` 主循环仍在运行，当前更像是 `ESP32` 真正工作后引发的 `3V3` 供电/显示链路异常，而不是整机完全死机。
- 2026-03-10 11:33 现场补充：用户说明当前 `ESP32` 开发板还单独插着自己的 `USB` 数据线供电。该信息推翻了上一条“`NUCLEO 3V3` 被 `ESP32` 拉垮”的假设；后续排查应优先避免“双路同时供电”，并把重点转回 `UART/接地/板载 USB-UART 干扰`。
- 2026-03-10 12:05 调试定位：通过 `ST-LINK` 在线调试确认，异常发生时并非单纯“页面卡住”，而是系统掉进了 `HardFault`。调用栈落在 `main -> oled_render -> send_cmd`，同时 `g_oled.hw` 在初始化后被运行时写坏。当前代码侧首先修正了两处高风险点：1) 禁止再把普通调试文本发进 `ESP32 AT UART`；2) `AT` 发送改为直接写串口寄存器，避开 `HAL_UART_Transmit` 与 `RX 中断` 共用同一 UART 句柄的状态竞争。新固件已重新烧录，待现场复测是否解除 `P1` 卡死。
- 2026-03-10 12:18 现场复测：在保持 `OLED + ESP32 USB供电 + GPIO16/17 UART + 共地` 的条件下，用户确认 OLED 已“**可以正常轮播，不再卡在 P1**”。这说明本轮代码修正已解除最关键的卡屏症状，下一阶段重点转为继续核对 `ESP32` 串口日志、联网状态和 `AP/云端` 行为。
- 2026-03-10 12:26 调试复核：通过 `ST-LINK` 再次读取现场运行态，确认 `g_mqtt.state = MQTT_STATE_ONLINE`、`g_at.state = AT_STATE_IDLE`、`g_at.urc_count = 0`，主循环停在正常的 `HAL_Delay(10)` 位置，说明当前设备侧已经跑到“联网在线”状态，不再卡死在 `CWJAP` 或 `HardFault`。
- 2026-03-10 12:28 现场复核：用户观察 OLED 轮播页中的 `NET=` 页面，明确看到 `NET=ONLINE`。至此，当前最小组合下已确认“屏幕轮播恢复 + 设备联网在线”两条主线跑通。
- 2026-03-10 17:55 应用侧联调：`Aquarium_APP` 已在 `API 17` 模拟器成功安装并能拉起首页。当前 DevEco Studio 的 `Run 'entry'` 按钮仍存在本地运行配置缓存异常，但应用本身已可通过 `hdc + aa start` 正常启动，说明阻塞点在 IDE 本地配置而不是 HAP 构建或应用代码。
- 2026-03-10 17:56 环境结论：`Aquarium_APP` 放在 `E:` 盘（`exFAT`）时，`ohpm` 的 `@ohos/hypium` 依赖入口容易异常，导致 `CompileArkTS` 报 `lstat ... hypium`。本轮已通过重建 `oh_modules` 后恢复构建，后续若再出现同类问题，应优先检查 `oh_modules/.ohpm` 与外层 `oh_modules/@ohos/*` 是否一致。
- 2026-03-11 00:40 新工程试验：已按用户要求把旧应用侧源码迁入新的 `aquariumApp/` 工程。迁移范围包含 `entry/src/main/ets`、`entry/src/main/resources`、`entry/src/ohosTest`、`entry/src/test`、`scripts`、`signing`，并把新工程的 `bundleName`、`module.json5` 权限、`build-profile.json5` 签名与 `API 17` 目标版本对齐到旧应用。
- 2026-03-11 01:05 新工程构建根因：迁移后的 `aquariumApp` 在 `C:\Users\Administrator\Projects\aquariumApp_ntfs` 首次构建时报 `Cannot find module '@ohos/hvigor-ohos-plugin'`。最终定位不是业务代码问题，而是用户级 Hvigor project cache 缺少该新路径对应的 `workspace\node_modules\@ohos\hvigor` 与 `hvigor-ohos-plugin` junction。补齐 `C:\Users\Administrator\.hvigor\project_caches\9aeacc6603ad5c0526bc30a61e773043\workspace\node_modules\@ohos\*` 两条 junction 后，新工程副本恢复可构建。
- 2026-03-11 01:17 新工程副本验证：`C:\Users\Administrator\Projects\aquariumApp_ntfs` 已成功构建出 `entry-default-signed.hap`，并可安装到 `API 17` 模拟器。应用启动日志确认 `Ability onCreate / onWindowStageCreate / Succeeded in loading the content` 均正常，新工程壳本身没有阻止页面启动。
- 2026-03-11 01:31 新工程副本结论：在新工程包里点击 `刷新` 后，界面仍然复现 `update failed`，状态区依旧是 `ONLINE=UNKNOWN`、时间字段为空。这说明“重建一个新 App project 再迁源码”不能解决当前刷新失败；阻塞点仍然在应用运行时请求链路，而不是旧工程脚手架本身。
- 2026-03-11 03:03 应用侧突破：`Aquarium_APP` 北向 REST 请求已打通。模拟器内实际日志已确认 `GET /devices/{id}` 与 `GET /devices/{id}/shadow` 都返回 `HTTP 200`，且抓到了真实云端返回体：影子中已有 `temperature=26.7 / ph=7.03 / tds=745.5 / turbidity=9.9 / water_level=59.7`。因此“App 连不上云”已不再是当前主问题。
- 2026-03-11 03:04 云端状态结论：同一轮日志里，设备信息接口返回 `status=OFFLINE`，而影子 `reported.event_time` 仍停在 `20251120T074327Z`。这说明当时云端侧仍在返回旧影子，不是当前桌面实物这一轮的实时上报；后续需继续核对“设备本地 ONLINE”与“云端是否真实在线/持续上报”的一致性。
- 2026-03-11 04:33 云控制台复核：通过控制台设备详情页确认 `690237639798273cc4fd09cb_MyAquarium_01` 当前状态已显示为“在线”，并已开启消息跟踪。这说明设备到云链路在本轮后段已恢复到云平台可见；接下来重点转为继续核对最新上报是否进入影子，以及应用前端卡片渲染与中文化收尾。
- 2026-03-11 04:40 设备侧代码修正：已将 `ESP32 MQTT` 状态机中的两条“假在线”路径改为保守重连：1) `MQTTSUB` 超时不再直接判定 `ONLINE`，改为进入 `ERROR`；2) `PUB_DATA` 阶段超时不再维持 `ONLINE`，改为进入 `ERROR`；同时在线态与命令轮询中补充了断链 URC 识别（如 `WIFI DISCONNECT / CLOSED / DISCONNECT / +MQTTDISCONNECTED`），命中即触发重连。
- 2026-03-11 04:55 设备侧验证：已使用本机实际 PlatformIO 路径 `C:\Users\Administrator\.platformio\penv\Scripts\platformio.exe` 重新完成 `run -e nucleo_f103rb` 与 `test -e native`。在清理损坏的 `.pio` 构建缓存后，固件构建成功，原生测试 `211/211` 全通过；随后已通过 `stlink` 成功烧录到现场板卡并自动复位。
- 2026-03-11 07:10 深入定位：通过 `ST-LINK + GDB` 在线抓运行态，确认旧版 `PUB_DATA` 路径里用于回灌 `URC` 的本地数组 `AtLine deferred_urcs[AT_URC_QUEUE_SIZE]` 会在栈上瞬时占掉约 `6KB+`，结合当前固件静态 RAM 占用后，足以在 `USART1 RX` 中断叠加时触发 `HardFault`。该问题已通过改成“按队列快照逐条回灌”修掉，本地构建与原生测试均已重新通过。
- 2026-03-11 07:24 新诊断结论：`HardFault` 去掉后，设备侧主阻塞点前移到 `ESP32 AT` 配置阶段。在线读取 `AtClient` 诊断字段已确认：最近失败命令不是 `MQTTPUBRAW`，而是 `AT+MQTTUSERCFG`；且即便把该命令简化为极短占位参数（`AT+MQTTUSERCFG=0,1,"a","a","a",0,0,""`），模块仍直接返回 `ERROR`。当前判断应继续围绕“ESP32 当前 AT 固件的 MQTT 指令支持/兼容性”排查，而不是继续怀疑 STM32 主循环或应用前端。
- 2026-03-11 07:46 现场独立运行复核：在彻底断开 `ST-LINK` 在线调试会话后，`LED1` 红绿交替闪立即消失，说明该现象仅由调试口通信触发。随后用户先后手动按下 `ESP32 RST` 与 `NUCLEO RESET`；其中 `NUCLEO RESET` 后，OLED `NET` 页表现为“先 `CONN`，随后回到 `OFF`”。这说明设备并非完全停机，而是能进入联网初始化阶段，但很快在后续 AT/MQTT 配置步骤失败并退回离线态，与前面抓到的 `AT+MQTTUSERCFG -> ERROR` 结论一致。
- 2026-03-11 08:01 在线状态补充：再次读取 `MqttClient` 运行态后，确认当前 `g_mqtt.state = MQTT_STATE_ERROR`，`g_at.state = AT_STATE_DONE_ERROR`，最近失败命令仍是 `AT+MQTTUSERCFG`，且 `reconnect_delay_ms` 已指数退避到 `60000 ms`。因此用户在 OLED 上连续观察 `40` 秒仍始终为 `OFF` 是符合当前状态机行为的，不是现场漏看；后续无需再长时间人工盯屏，优先继续修复 `MQTTUSERCFG` 的 ESP-AT 兼容问题。
- 2026-03-11 08:18 ESP32 固件处置：已通过 `esptool` 在 `COM9` 上重新整包刷入官方 `ESP32-WROOM-32-AT-V4.1.1.0`，分区写入与哈希校验全部通过。刷完后重新复核发现：`STM32` 侧不再立即死在 `HardFault`，但在本轮现场中仍会出现“先正常轮播，十几到二十秒后页面卡在 P1”的体验。
- 2026-03-11 08:26 新代码修正：1) `AT` 引擎将 `+CIPSNTPTIME:` 提升为高优先级 `URC`，避免时间行被后续噪声挤掉，已补回归测试 `test_mqtt_sntptime_survives_noise_after_time_line`；2) `OLED` 驱动改为“单次 I2C 写失败立即结束本帧”，同时主程序里的 `oled_i2c_write` 将单次超时从 `100ms` 下调到 `10ms`，并在失败后立即重置 `I2C1` 外设，避免整机长时间卡在阻塞式刷屏路径。对应本机验证已重新通过：`run -e nucleo_f103rb` 成功，`test -e native` 更新为 `217/217` 全通过，并已重新烧录。
- 2026-03-11 10:40 现场复核：用户确认本轮最新固件下，`OLED` 已经恢复为“**不卡**”，且 `NET=ONLINE`。同时，通过 `chrome mcp` 直接查看华为云控制台，设备详情页显示“在线”，最近上线时间已更新到 `2026/03/11 10:40:37 GMT+08:00`。
- 2026-03-11 10:41 云端补充结论：虽然消息跟踪里已出现新的“平台数据转发 / 流转规则触发”记录（如 `2026/03/11 10:40:38 GMT+08:00`），但设备影子页中的“最新更新时间”仍停在 `2025/11/20 15:43:27 GMT+08:00`，属性值也仍是旧数据。这说明当前设备到平台已经有新消息进入平台侧链路，但这些消息仍未正确落入 IoTDA 设备影子。
- 2026-03-11 10:52 现场变化：用户在这一轮联调中已将设备拔电，随后观察到 `NET=OFF`。该状态按现场操作应视为“断电结果”，不是新的联网回退结论；下一步需先恢复最小闭环供电，再继续验证“设备在线但影子未更新”这条主线。
- 2026-03-11 10:54 现场复核：用户重新插回 `NUCLEO USB + ESP32 USB` 后，未按 `RESET` 的情况下，OLED 出现“屏幕卡住、数据不动、不再轮播”的现象。该结果说明当前桌面链路在热插回供电后不能稳定自行恢复到干净启动态；后续应默认在恢复供电后通过一次人工 `NUCLEO RESET` 重新拉起主循环，再继续观察联网状态。
- 2026-03-11 11:02 现场复核：在保持当前最小闭环接线不变的前提下，用户将 `OLED` 的 `VCC` 从 `3V3` 拔下再重新插回后，显示链路恢复，页码重新开始轮播，且 `NET` 页重新显示为 `ONLINE`。这说明当前“屏幕卡在 P1 / 画面不刷新”与 `OLED` 模块侧供电/显示链路状态直接相关，不等同于主循环或 MQTT 主状态机停机。
- 2026-03-11 11:48 设备侧最终定位补充：已对照 Espressif 官方 `ESP-AT v4.1.1.0` 文档确认 `AT+MQTTUSERCFG` 语法与 MQTT 命令支持均正常，且在 `COM9` 直连实测时真实长命令也能返回 `OK`。随后通过在线诊断字段确认：设备侧真正卡点并非 MQTT 命令本身，而是属性上报前的本地 JSON 构造。旧实现使用单次 `snprintf` 拼接含浮点字段的 payload，在板端运行时反复返回 `AQUA_ERR_BUFFER_TOO_SMALL`，导致设备一直在线但从未真正进入属性上报。现已将属性上报 JSON 改为手工分段拼接，避开运行时浮点格式化问题，并重新完成构建与烧录。
- 2026-03-11 20:43 直连 ESP32 复核：已把 `ESP32-WROOM` 临时刷入 `release/factory_WROOM-32-uart0.bin`，并在 `COM14` 上直接复现完整 `AT` 序列。结论有两条：1) MQTT 鉴权时间戳必须使用 `UTC YYYYMMDDHH`，若误用 `timezone=8` 本地时区，会在 `AT+MQTTCONN` 阶段直接出现 `+MQTTDISCONNECTED:0` + `ERROR`；2) 改回 `UTC` 后，官方 `ESP-AT v4.1.1.0` 在 `UART0` 上可稳定完成 `AT+MQTTCONN`、`AT+MQTTSUB`、`AT+MQTTPUBRAW`，并实测返回 `+MQTTPUB:OK`，云端影子也已更新到 `temperature=26.5 / ph=0 / tds=0 / turbidity=0 / water_level=80 / event_time=20260311T124659Z`。
- 2026-03-11 21:10 现场链路再定位：将 `ESP32` 刷回 `release/factory_WROOM-32-no-flow.bin`（`UART1 tx=17 rx=16`）并重新烧录最新 `STM32` 固件后，华为云 `connection_status_update_time` 仍会周期性前跳，但 `shadow.reported.event_time` 不再继续更新；在线读取运行态显示业务侧已切到紧凑版 payload，但现场 `UART1` 方案仍未把属性持续推入影子。当前最强判断：问题已收敛到 `STM32 <-> ESP32 UART1(16/17)` 这条现场 AT 链路，而不是华为云鉴权、Topic、Payload 格式或 App 取值链路。
- 2026-03-11 21:48 现场复位复核：用户已重新接回 `ESP32 17 -> NUCLEO D2(PA10)`、`ESP32 16 -> NUCLEO D8(PA9)`，随后按 `NUCLEO RESET` 后反馈 `OLED` 页码可以继续轮播，且 `NET=ONLINE`。这说明当前 `16/17` 串口链路至少已恢复到“能启动、能联网、不会立刻卡死”的状态。
- 2026-03-11 21:51 云端同步复核：在上述 `OLED 轮播 + NET=ONLINE` 状态下，应用侧直查华为云影子仍返回旧的 `reported.event_time=20260311T124659Z`，`version=28384`，属性值保持 `temperature=26.5 / ph=0 / tds=0 / turbidity=0 / water_level=80 / alarm_level=0` 不变；但 `connection_status_update_time` 已更新到 `2026-03-11T13:50:28.322Z`。这说明“设备在线”与“影子持续刷新”仍未完全闭环，问题尚未解除。
- 2026-03-11 21:55 现场掉线复核：在 `ESP32 17 -> D2`、`ESP32 16 -> D8` 重新接回、`NUCLEO RESET` 后，用户观察到 `OLED` 页码可轮播，且初始 `NET=ONLINE`；继续盯 `40` 秒后，`NET` 页面转为 `OFF`。在线抓运行态显示：设备当前掉在 `MQTT_STATE_ERROR`，`AT_STATE_DONE_ERROR`，最近失败命令已前移为 `AT+MQTTPUB`（不再是 `MQTTPUBRAW`），最近一次待发 payload 已压缩为仅含 `temperature / water_level / alarm_level` 的紧凑版。这说明当前现场链路在“连上云”之后，仍会在属性发布这一跳被 `ESP32 UART1(16/17)` 方案打断。
- 2026-03-11 21:58 现场复位对比：在 `NET=OFF` 状态下，用户单独按一次 `ESP32 RST`，`15` 秒后 `NET` 仍为 `OFF`；随后只按一次 `NUCLEO RESET`，`15` 秒后 `NET` 恢复为 `ONLINE`。这说明当前问题不属于“ESP32 自己卡死后单独复位即可恢复”，而更像是 `STM32` 侧需要重新走完整初始化链路（`AT+RST -> SNTP -> MQTT`）后才能短暂回到在线态。
- 2026-03-12 00:00 现场新尝试：已把 `ESP32` AT 命令口切换到 `GPIO23/GPIO18`，同时把 `STM32 USART1` 与 `ESP32` 都降到 `57600`。用户按 `NUCLEO RESET` 后，`NET` 仍表现为“先 `ONLINE`，随后掉到 `OFF`”，说明问题并不只由 `GPIO16/17` 或 `115200` 波特率引起；当前故障模式仍是“可短暂上线，但无法长期维持属性发布闭环”。
- 2026-03-12 02:08 设备侧关键修正：在线诊断抓到 `STM32` 发往 `ESP32` 的 `AT+MQTTPUBRAW` 长度参数被错误格式化成了字面量 `zu`，导致模块收到的发布命令天然非法并直接返回 `ERROR`。现已将相关 `size_t` 文本格式化统一改为十进制字符串，`MQTTPUBRAW/CIPSEND` 两处都已修正，本机 `native` 测试 `219/219` 通过并已重新烧录。
- 2026-03-12 02:16 闭环恢复：在 `ESP32 GPIO23 -> NUCLEO D2(PA10)`、`ESP32 GPIO18 -> NUCLEO D8(PA9)`、`57600` 波特率配置下，用户实测 `NET` 页可持续保持 `ONLINE`；同时应用侧页面已显示与 OLED 一致的数据。随后直查华为云影子确认：`reported.event_time` 已更新到 `20260311T180806Z`，`version=28598`，说明“设备在线 + 属性上报 + App 刷新展示”最小稳定闭环已重新跑通。
- 2026-03-12 03:32 传感器复接确认：`DS18B20` 已重新接回并通过实测验证。用户手捏金属探头 `10` 秒后明确反馈“温度有上升”；同时云端影子最新值已更新到 `temperature=25.69`、`event_time=20260311T193206Z`。这说明当前 `温度探头 -> STM32 -> 云端 -> App/OLED` 这一条链路已经稳定打通。
- 2026-03-12 03:40 传感器复接确认：`水位传感器` 已重新接回 `VCC->3V3`、`GND->GND`、`AO->A3(PB0)`。用户按 `NUCLEO RESET` 后，现场手动执行“离水 / 浸水”测试，明确反馈“水位会变化，且百分比和浸入程度基本一致”。这说明当前 `水位传感器 -> STM32 -> OLED` 这一条链路也已恢复到可演示状态。
- 2026-03-12 03:52 pH 复测结论：按旧基线 `V+->3.3V`、`G->GND`、`Po->A0(PA0)` 重新接回后，用户按 `NUCLEO RESET` 观察到 `OLED` 上 `pH` 在空气中约 `2.8~3.0`，电极放入自来水后仍为 `2.8~3.0`，没有出现应有的明显变化。结合前面多轮复测结果，可继续按“模块/电极链路异常，非当前主线代码问题”处理。
- 2026-03-20 08:10 执行器自检进展：已给 `STM32` 刷入临时“舵机上电自检”固件，理论行为为 `中位 -> 一端 -> 另一端 -> 回中`。用户按 `NUCLEO RESET` 后明确反馈“舵机没扫动”。当前不能直接判定是代码问题，需继续先区分 `供电 / 共地 / 信号线 / 舵机本体` 中哪一环异常，再决定是否切到继电器空载测试。
- 2026-03-20 08:16 舵机进一步复核：用户手捏舵机外壳、再次按 `NUCLEO RESET` 时，能听到“非常微弱的齿轮转动声”。这说明当前 `PWM` 信号和舵机供电并非完全断路，舵机至少收到了一点动作指令；后续优先继续排查 `机械卡滞 / 供电能力不足 / 舵机本体力矩异常`，而不是先判定主控代码没有输出。
- 2026-03-20 08:22 舵机空载复核：用户已拆掉摇臂/机构件并再次按 `NUCLEO RESET`，反馈“响声也没了”。这说明问题不属于外部机械卡滞，当前更应优先怀疑 `舵机 5V 供电不足 / 接触不良 / 舵机本体异常`。
- 2026-03-20 09:20 下行命令链路补充：已将 `ESP32` 临时切回 `UART0` 直连诊断，并在 `COM15` 上用官方 `ESP-AT v4.1.1.0` 完整完成 `Wi-Fi -> SNTP(UTC) -> MQTTCONN -> MQTTSUB($oc/devices/{device_id}/sys/commands/#)`。随后通过应用侧同步命令 API 再次下发 `feed=true`，云端仍返回超时，但直连串口在 `45` 秒等待窗口内没有出现任何 `+MQTTSUBRECV`。结合官方文档确认订阅 Topic 写法正确，可继续判断当前问题更偏向“平台命令投递/会话侧”而非 `STM32` 命令解析或命令响应 JSON 构造。

## 4. 当前适合继续实测的最小闭环

当前最稳的实物组合是：

1. `NUCLEO + OLED + ESP32 + DS18B20 + 水位传感器`
2. 先验证 `OLED显示 -> Wi-Fi上线 -> 云端在线/影子刷新`
3. 再追加 `pH/TDS/浊度`，不要一次把所有可疑模块都挂上

## 5. 备注

- 标准接线仍以 `docs/Wiring_Guide.md` 为准。
- 本文档用于记录“当前桌面联调事实”，后续每轮排障后都应继续补充。

